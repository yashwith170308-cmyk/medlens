# MedLens Clinical Intelligence System Configuration
# ₹0 / $0 Cost Constraint — Fully functional without any paid services or API keys

PORT=5000
NODE_ENV=development

# Optional: Google Gemini API Key for AI Summary (Free Tier)
# If left empty, MedLens uses its deterministic, rule-based clinical engine (100% offline & ₹0 cost)
# GEMINI_API_KEY=your_free_gemini_api_key_here
